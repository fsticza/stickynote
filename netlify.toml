[build]
  publish = "dist"
  command = "npm run build"
[build.environment]
  NODE_VERSION = "18"
[[headers]]
  # Define which paths this specific [[headers]] block will cover.
  for = "/img/*"
  [headers.values]
	cache-control = '''
	public,
  max-age=604800,
	immutable'''
